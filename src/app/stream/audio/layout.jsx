export const metadata = {
  title: "Audio del stream",
  description: "Fuente privada de reproducción de audios para OBS.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function StreamAudioLayout({ children }) {
  return children;
}
